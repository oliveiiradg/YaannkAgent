# Config do docker-compose da Evolution API. Copie para deploy/evolution/.env e preencha.
# O deploy/evolution/.env NÃO vai para o git.

POSTGRES_USER=evolution
POSTGRES_PASSWORD=
POSTGRES_DB=evolution

# Chave de autenticação da Evolution API (o gateway usa a mesma em EVOLUTION_API_KEY).
EVOLUTION_API_KEY=
